# リポジトリ運用ルール

## 修正の進め方(plan.md ワークフロー)

修正(機能追加・変更・バグ修正などファイル変更を伴う作業)は、次の手順で進める:

1. **要求を元に `plan.md` を作成する**(リポジトリルートに置く)。内容: 要求の要約、変更対象、実装方針、検証方法、スコープ外
2. **人間が plan.md を読んで go を出してから実装を開始する**。go が出るまでファイル変更・実装委任に着手しない
3. **plan.md と実装に差分が生じたら、その都度 plan.md に明記する**(「当初計画からの変更」として、何をなぜ変えたかを追記。実装後に plan.md が実態と一致している状態を保つ)
4. **最終的な plan.md はファイルとしてコミットしない。その内容をそのままコミットメッセージとして使用する**(`git add` に含めない。コミット後の plan.md は削除してよい)

補足:

- delegate スキルで実装を委任する場合も、plan.md の go が委任開始の前提(plan 承認 → 指示書作成 → 委任 → レビュー → plan.md をコミットメッセージにしてコミット)
- ごく軽微な修正(誤字修正など、結果が一意で説明不要のもの)は plan.md を省略してよい。迷ったら作る
